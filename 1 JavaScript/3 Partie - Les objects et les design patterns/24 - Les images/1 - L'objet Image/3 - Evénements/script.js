var myImg = new Image();

myImg.addEventListener('load', function (){
    // Etape 1 : on modifie notre événement
    //Etc
});

// Etape 2 : on spécifie l'adresse de notre image
myImg.src = 'logo.jpg';